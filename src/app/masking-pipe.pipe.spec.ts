import { MaskingPipePipe } from './masking-pipe.pipe';

describe('MaskingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MaskingPipePipe();
    expect(pipe).toBeTruthy();
  });
});
